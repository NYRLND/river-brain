# Full dev environment: production deps + notebook, fit report and tests (Python 3.14).
# SciPy is intentionally absent: the only thing we needed (a Hilbert transform) is 6 lines of numpy.
-r requirements-prod.txt
matplotlib==3.11.2
jupyter==1.1.1
nbconvert==7.17.1
ipykernel==7.3.0
jupytext==1.19.5
pytest==9.1.1
